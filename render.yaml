services:
  - type: web
    name: nexus-chat-app
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd backend && npm install && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
